/*
import React from 'react';
import classes from './Friends.module.css';



const Friends = (props) => {

	let friendsElement = props.state.friends.map(dialog =><DialogItem name={dialog.name} id={dialog.id} avatarName={dialog.avatarName} />);


   return (
   	<div className={classes.friends}>
	    <div className={classes.circle}>
		    <NavLink to="/friends">Friends</NavLink>
	    </div>
	    <div className={classes.circle}>
		    <NavLink to="/friends">Friends</NavLink>
	    </div>
	    <div className={classes.circle}>
		    <NavLink to="/friends">Friends</NavLink>
	    </div>

	   {/!* <div className={classes.circle}>Friends</div>
*!/}
    </div>
   );
};

export default Friends;*/
